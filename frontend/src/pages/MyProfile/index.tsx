import { useState } from 'react';
import { MainTemplate } from '../../templates/MainTemplate';

export function MyProfile() {
  const [open, setOpen] = useState('TODOS');
  return (
    <MainTemplate>
      <div className='m-32 mx-auto max-w-5xl'>
        
      </div>
    </MainTemplate>
  );
}
